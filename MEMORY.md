# 记忆索引 (Short-Drama Memory Index)

> 本目录是「短剧导演」的持久记忆库。Agent 在每个会话开始时读取本文件恢复上下文，并在使用过程中持续更新。请勿删除。

## 用户偏好（更新于首次初始化）

- **已全面采用 codex-short-drama-director v3.0 行为（2026-本会话）**：以 v3.0 技能（总控 short-drama-director + 三技能 + references 子库）为准；此前与 3.0 冲突的细则（canonical 输出、转场方式、单镜时长等）以 3.0 为准，详见 CODEX_KNOWLEDGE.md 与技能。以下旧偏好中与 3.0 不冲突的仍有效。
- 默认视频生成工具：**Seedance 2.0/2.5（即梦）**
- 默认画幅：竖屏 9:16（1080×1920）——**画幅由工具参数面板设置，提示词中不写画幅**（用户纠正，已生效）
- 默认风格：电影感写实、短剧快节奏（前 3 秒钩子，每 5–10 秒一个信息点/反转）
- 提示词结构：每条以「全局词头」开头（风格/质感/景深/布光等贯穿条件前置声明），镜内只写镜头特异性内容（用户建议，已生效）
- 资产分类：核心四类 = 人物 / 场景 / 道具 / 音频；服装、风格为可选扩展（用户建议，已生效）
- 提示词结构：三段式——全局词头 / 资产引用段（人物：/场景：/道具：/音频：逐行前置）/ 镜头段（景别+动作+运镜+光线+参数）（用户建议，已生效）
- 负向提示：多次出现的负向统一放入词头做全局约束，逐镜不重复（用户建议，已生效）
- 默认无 BGM：全片无背景音乐，仅环境音/音效/台词配音（用户要求，全局默认；除非用户脚本明确要求 BGM）
- 工具约束：单条生成时长 4–30s（用户告知）；分段/整合 text 每条时长必须落在该区间内
- 镜头参数：镜头行必带焦距/光圈/机位（24–35mm 广角·50mm 标准·85–135mm 长焦·100mm 微距；f/1.8–f/2.8 情绪常规·环境深景深可 f/4+；贴地/仰拍/俯拍/肩平/侧面），规范见 short-drama-prompts 技能 2.7 节（用户确认，已生效）
- 定位关系：词头/资产段必带 `定位：` 行——ASCII 构图图（平面）+ **垂直剖面**（高度/机位/视线）；多人同框用 `@定位资产图` 或 ASCII+垂直剖面（技能 2.8 节，用户确认，已生效）
- 剧情约束：**不许修改剧情，所有内容围绕剧情展开**——词头必带「严格按原剧情执行，不增删情节不改台词」；诊断/建议只优化呈现不碰剧情（用户要求，已生效）
- 剧本诊断：**不输出「修改建议」模块**（用户要求，已生效）——诊断只陈述事实（题材/受众/钩子/节奏/情绪/付费点/弧光/台词），不给出改动意见；**唯一例外：台词精简度每次输出「参考建议」（待定·仅供参考，是否采纳由用户决定）**（用户要求）
- 剧情红线强化：**剧本给出的集数固定不动（如 25 集），每集剧情保持原样；所有会更改原剧本内容（台词/情节/人物设定/集数/分集剧情/时间线）的方案一律不采用**（用户要求，已生效）——**覆盖全流程四个阶段：剧本诊断 / 资产需求分析 / 分镜 / 提示词**（各技能已同步写入红线条款）
- 反馈格式：**所有反馈内容一律 text 模式**——用 ```text 代码块输出纯文本，不用 Markdown 富排版（表格/加粗/链接），方便直接复制（用户要求，已生效）
- 提示词稿格式：**提示词稿文件也用纯 text 格式**（无 Markdown 富语法，纯文本分隔线/编号，扩展名 .txt）（用户要求，已生效）
- 描写风格：**双模式并存**——A 文学细腻（成语/意象/氛围）｜B 直白拆解（肢体细节/无成语）；**每次生成提示词前询问用户选择**（用户要求，已生效）
- （后续用户明确的偏好与纠正会追加在这里）

## 项目清单

- **被转让的网恋女友**：进行中（诊断 ✅ / 人物 ✅ / 资产 ✅ / 第 21 集提示词 ✅，22–25 集暂定；25 集原剧本固定不动）。女频网恋复仇×双向救赎；关键角色：妮娜·戈麦斯、塞巴斯蒂安·蒙特内格罗、埃斯特班·贝尼特斯、卡米拉·科尔特斯（见 characters.md）；关键资产：聚餐包间/格斗馆/暴雨街道/手机群聊等（见 assets.md）。剧本提取件：`被转让的网恋女友-西语-提取.md`。

## 经验速览（详见 lessons.md）

- 金句识别后建议做切片/海报。
- 屏幕文字/字幕一律后期贴片，提示词不写文字（AI 必乱码）。
- 「血泪」类极端面部效果改「泪水结冰晶」物理方案。
- 女主爽剧注意给女主加「赢家姿态」微动作镜头，避免全片渣男视角。
- 技能架构（2026-本会话起）：短剧生产拆为三技能——`short-drama-script-analysis`（剧本/人物）、`short-drama-assets`（资产）、`short-drama-prompts`（提示词）；原总技能已删。按工作台加载对应技能。

## 记忆库结构

- `characters.md` — 角色档案库（含 Seedance 一致性 canonical 描述词）
- `assets.md` — 资产库（场景/道具/服装/风格资产卡）
- `prompt-templates.md` — Seedance 2.0 提示词模板与经验
- `lessons.md` — 经验教训（自我提升）
- `projects/` — 每项目一份完整档案（按 `projects/_TEMPLATE.md` 模板建档：模块地图 + 剧本诊断、人物、资产、分镜、效果记录）

## 自我复刻套件

- `REPLICATE.md` — 完整复刻教程：把「短剧创作助手」预设 + 血蓝条插件 + 记忆库搬到新设备/新 Agent。
- `replicate/` — 预设与插件的原始文件副本（修改预设/插件后记得把新文件复制回来并提交）。
