# Seedance 2.0 提示词模板库

> 维护者：`short-drama-prompts` 技能（提示词生成）｜跨项目沉淀库
> 统一结构：**[全局词头] + [资产引用段：人物/场景/道具/音频] + [镜头段：景别/动作/运镜/光线/参数]**。**画幅不在提示词中写**（即梦参数面板设置，默认 9:16 竖屏）。

## 全局词头（贯穿全片条件，每条提示词开头必带）
- 词头 = 全片不变的风格/质感/景深/布光 + 项目级全局要求（如「无背景音乐，仅环境音/音效/配音」）+ **剧情约束（严格按原剧情执行，不增删情节不改台词）**，前置声明一次，镜内不重复。
- 词头模板：`电影感写实，胶片颗粒，浅景深，真实质感，电影级布光，细节丰富。无背景音乐。严格按原剧情执行，不增删情节不改台词。`
- 换风格时整体替换词头（写实→国风/动漫/赛博等），镜头段结构不动。
- **负向入词头（全局约束）**：多次出现的负向提示统一放进词头，逐镜不重复。默认：`负向：变形、多指/六指、面部崩坏、文字乱码、屏幕字体变形、画面闪烁、抽帧、多余肢体、前后镜头不一致、眼珠变形`

## 资产引用段（本镜资产前置声明，只列涉及的类型）
- `人物：<姓名>，<canonical 描述词>`（characters.md，去尾部风格词）
- `场景：<资产名>，<固定描述词>` ｜ `道具：<资产名>，<固定描述词>`（assets.md）
- `音频：<资产名>，<声音要点>`（BGM 等全局音频在词头/稿头声明，不逐镜重复）
- 资产引用集中声明 → 模型把固定描述词当作形象/场景锁定条件，镜头段只管动作与调度

## Seedance 2.5 时间轴分镜模板（多镜头时间轴，单条 4-30s）
```text
<风格与光影节奏，如：中式权谋家庭压迫感，写实电影质感；午后阳光斜照，暖光里带冷；镜头稳定不快闪>。无背景音乐。严格按原剧情执行，不增删情节不改台词。
负向：变形、多指/六指、面部崩坏、文字乱码、画面闪烁、抽帧、多余肢体、前后镜头不一致、眼珠变形、字幕、文字。
人物：<姓名>@<资产图>（锁脸）。
场景：<资产名>，<固定描述词>。
音频：<SFX 明细>。
镜头：<时间轴，如0.0–2.4s> <景别> <画面+动作> <运镜> <光线>。时长<Xs>。
```
- 头部五段声明：风格（光影/节奏）→ 场景 → 人物（@资产图锁脸）→ 约束（无字幕/无旁白朗读）→ 音频（SFX；**默认无 BGM**）
- 旁白/台词独立成行（后期配音），**不写进生成提示词**
- 无字幕约束：词头声明「无字幕无文字」+ 负向追加「字幕、文字」
- 末镜可写「最后 0.4 秒画面定格不动」便于剪辑截取

## 描写风格双模式（每次生成前询问）
- **A 文学细腻**：允许成语/意象/氛围渲染（如「青筋暴起」「一片死寂」「气场压人」），强调情绪质感
- **B 直白拆解**：动作/表情拆到肢体细节（如「喉咙血管鼓起来」「手指陷进雪泥」「呼吸明显变急促」），不用成语
- 每次生成前用 ask_user_question 询问；两版结构（词头/资产/定位/镜头参数/台词/剧情约束）完全一致，仅镜头行动作表情措辞不同

## 定位关系（词头/资产段必带）
- 格式：`定位：<@定位资产图（人物站位参考）> 或 <ASCII 构图图 + 垂直剖面>`（用户规范）
- ASCII 构图图：竖框=9:16 画面边界，框内标注元素方位（前/中/背景、左/右/中），`→` 标光源方向；**ASCII 只表达平面，必须追加「垂直剖面」行**：元素高度（m）、机位高度、视线方向（仰/俯/平视）
- 示例：
```text
定位：
┌─────────────────────────┐
│      窗户(背景·自然光)     │
│        ┌──────┐         │
│        │[角色B]│(右·中景)  │
│        └──────┘         │
│        视线→(右→左)      │
│  [角色A](左·前景·坐姿)     │
└─────────────────────────┘
垂直剖面：窗户(≈2m) > 角色B站姿(≈1.7m) > 角色A坐姿(≈0.9m) > 地面(0m)；镜头肩平
主光：窗光（左→右）· 自然冷调· 间距约两米
```

## 镜头参数词典（镜头行必带）
- 格式：`<时间轴> <景别> <机位/角度>，<焦距> f/<光圈>，<画面细节>`
- 景别：大远景ELS｜远景LS｜全景FS｜中全景MLS｜中景MS｜中近景MCU｜近景CU｜特写BCU｜大特写ECU｜微距
- 焦距：24–35mm 广角（空间/冲击）｜50mm 标准叙事｜85–135mm 长焦（情绪压缩/特写）｜100mm 微距（细节放大）
- 光圈：f/1.8–f/2.0 浅景深情绪特写｜f/2.8 常规叙事｜f/4+ 深景深环境交代
- 机位高度：贴地/低机位/腰位/胸位/肩平/高机位/顶拍/航拍
- 角度：平视/仰拍15°30°45°/俯拍15°30°45°/荷兰角/过肩OTS/主观POV/正反打
- 运镜：固定/推/拉/摇/移/跟/环绕/甩镜/手持/斯坦尼康/摇臂/轨道/升降/变焦
- 构图：三分法/中心/对称/对角线/引导线/框架式/留白/前景遮挡/俯仰透视
- 光学：广角畸变/长焦压缩/鱼眼/移轴/柔焦/眩光光斑/暗角/探针镜头
- 光影：伦勃朗光/侧逆光/轮廓光/顶光/底光/剪影/柔光/硬光/冷暖对比/高调/低调
- 节奏：慢动作/快切/变速/延时/抽帧/定格
- 转场：硬切/匹配剪辑/遮挡转场/缩放转场/甩镜转场/叠化/闪白闪黑/声音先入
- 动作描述原则：按时间先后（先环境→再动作→再反应，用先/随后/接着/最后）；景别与细节匹配（全景不写特写细节）；动作量与时长匹配（塞不下拆镜）；**环境与人物分开写（「环境：」静态背景/光线/陈设，「人物：」动作神态，各成一行不穿插）；环境动态（风/光影/雨雪）归环境行，与动作同时发生时用「与此同时/伴随」绑定**

## 通用模板
```text
电影感写实，胶片颗粒，浅景深，真实质感，电影级布光，细节丰富。无背景音乐。严格按原剧情执行，不增删情节不改台词。
负向：变形、多指/六指、面部崩坏、文字乱码、屏幕字体变形、画面闪烁、抽帧、多余肢体、前后镜头不一致、眼珠变形。
人物：<姓名>，<canonical 描述词>。
场景：<资产名>，<固定描述词>。
音频：<资产名>，<声音要点>。
定位：<ASCII 构图图 + 垂直剖面>。
镜头：<时间轴> <景别> <机位/角度>，<焦距> f/<光圈>，<具体动作> <运镜+速度> <光源色调>。时长<Xs>。
```

## 项目沉淀的有效写法（通用）
1. **屈辱/卑微感**：低机位仰拍 + 具体砸地/跪姿动作 + 冷色主调 + 极慢动作。
2. **金句卡点**：利用遮挡物（车窗/门框/玻璃）缓缓升起/关闭 + 反光中冷暖分隔 → 遮挡转场天然成立。
3. **时间跳跃锚点**：远景环境（烟花/季节变化）+ 字幕贴片（AI 不生成字幕）。
4. **屏幕/文字类**：一律后期贴片；提示词写「屏幕反光看不清细节」+ 负向「文字乱码、屏幕字体变形」。

## 负向提示（通用）
变形、多指/六指、面部崩坏、文字乱码、画面闪烁、抽帧、多余肢体、前后镜头不一致、眼珠变形。

## 经验（有效/无效，详见 lessons.md）
- 有效：遮挡转场（车窗/车灯过曝）出片稳定；冷暖双色调对比情绪力强。
- 无效/待验证：「血泪交织」类极端面部效果（已改冰晶方案）；屏幕文字直出（必乱码）。
