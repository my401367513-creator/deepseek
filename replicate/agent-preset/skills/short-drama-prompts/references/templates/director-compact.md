# 导演偏好紧凑提示词模板

本文件只定义可复制排版。所有判断与约束以规则模块为准；没有实际内容的可选栏直接省略。

```text
第<X>集
工具：<项目指定工具与版本；无则省略>
时长状态：<暂估，待正式音频锁定／正式音频已锁定>
前文入场状态：<时间、地点、人物、动作、情绪、道具>
本集信息边界：<本集可揭示内容>
后文承接要求：<结束时必须保留的状态>

第<X>集｜生成段<i>/<N>｜总时长<X.X>秒
<本段镜头数：1–8镜>

制作参考（不得提交视频模型）
【镜头一（0.0–X.X秒）】对应“<原文旁白或对白>”
【镜头二（X.X–X.X秒）】对应“<原文旁白或对白>”
<连续镜头共用一句原文时标明覆盖范围，不复制原句>

视频模型提示词

【第<X>集统一词头·严格锁定】
<真人写实默认或项目确认媒介>，<项目地域、时代、题材、美术、色彩、光影和镜头气质>，<固定、手持、跟拍的使用边界>。无背景音乐；不生成旁白；不生成字幕、字卡或需要准确阅读的画面文字。

<场景名称>：场景参考：@imageN；
<固定场景完成引用后不追加外观描写；无参考场景只在集中资产区写一次必要固定描述>。

<人物名称>：形象参考：@imageN，面部参考：@imageN，声音参考：@音频N；
<固定人物完成引用后不追加形象描写；临时无参考人物按实际镜头写必要文字提示>。

<道具名称>：道具参考：@imageN；
<固定道具完成引用后不追加外观描写；无参考道具只在集中资产区写一次必要固定描述>。

<以上风格、场景、人物和道具必须位于同一个统一词头标题下；每个生成段在此位置原样完整重复词头，使本段可独立复制提交；不得写“同上”或省略；没有关键道具时省略道具行>

【本段资产状态】
人物：<仅在有人物出镜或人物画外连续对白时保留；资产名＋动态状态>。
场景：<场景资产名＋当前时段、光线、人物位置、路径和连续性>。
道具：<仅在实际使用道具时保留；资产名＋持有人、持握、位置、交接和起止>。

【空间与承接】
<左右、前后、高低、面向、距离、视线轴>。摄影机：<机位侧别、高度、观察方向、焦距范围和运动状态>。上一段结束快照：<姿态重心、视线、接触、持握、道具、光源、动作阶段和跨切声音；首段写初始状态>。

【通用负向】
<简短高频生成风险>。

【本段针对性负向】（仅在存在额外真实风险时保留）
<只写本段额外风险>。

【镜头一（0.0–X.X秒）】
<景别与主体>，摄影机位于<人物／场景锚点的哪一侧或后方>、与<眼睛／胸口／腰部／地面>等高，从<观察方向与角度>以<焦距>拍摄<实际构图范围>；能看到<初始状态>，先<触发动作>，随后<连续可见变化>，最后<结束状态>；<同期对白或人物名画外连续对白及口型归属>。<仅在本镜确有需要时补充运镜、道具、声音、光影、动作阶段、声音承接或段尾硬切落点>。

【镜头二（X.X–X.X秒）】
<按同一单段结构续写；用“先、随后、接着、最后、与此同时”保持动作次序；镜头数量由内容决定>

长镜头报告（存在超过4秒镜头时保留）
镜头<X>：<X.X秒>；对应原文：“<原句>”；使用理由：<为何不能自然拆分>；可拆方案：<机位与自然切点>；确认状态：<待导演确认／已确认保留>。
```
