# 经验教训（Lessons）

## 2026-本会话 · 技能架构与规范演进

### 技能架构
- 用户要求把「剧本人物分析 / 资产需求分析 / 提示词生成」拆为三个独立技能。已执行：
  - `short-drama-script-analysis`（剧本诊断+人物档案，写 characters.md、projects 诊断/人物部分）
  - `short-drama-assets`（资产库设计，写 assets.md、projects 资产部分）
  - `short-drama-prompts`（Seedance 提示词规范，写 prompt-templates.md、lessons.md）
  - 原总技能 `short-drama-production` 已删除；persona 与 REPLICATE.md、复刻套件 replicate/ 已同步。
- 经验：模块化技能 + 各自内嵌记忆库协议（公共部分精简、负责文件明确），比单一大技能更利于按需加载与并行协作。
- 项目档案约定：新项目按 `projects/_TEMPLATE.md` 模板建档（三技能模块地图 + 各章占位），由 short-drama-script-analysis 技能在建档时执行。

### 提示词规范演进
- 资产分类：核心四类 = 人物/场景/道具/音频（用户建议）；音频资产卡含「用途与情绪/对应镜头/制作方式/声音要点」字段。
- 提示词结构：三段式——全局词头 / 资产引用段（人物/场景/道具/音频逐行前置）/ 镜头段（景别动作运镜光线时长）。资产引用集中声明利于模型锁定形象与场景。
- 全局约束：① 词头「无背景音乐，仅环境音/音效/配音」；② 负向提示并入词头做全局声明，逐镜不重复。词头 = 正面全局条件 + 项目级全局要求 + 负向行。
- 学习 Seedance 2.5 时间轴分镜模式（用户示例脚本）：头部五段声明（风格→场景→人物@资产图锁脸→约束→音频）、时间轴逐镜（旁白独立后期配音不进提示词）、无字幕约束（词头+负向加「字幕、文字」）、末镜定格收尾便于剪辑。已写入技能 2.6 节、prompt-templates.md 模板。工具记录：用户环境使用 Seedance 2.5（即梦）。
- 交付方式：镜头整合进同一条 text（多镜头时间轴叙事）；按段落分段交付（段内时间轴从 0 起标注全片时间）。
- 工具约束：单条生成时长 4–30s——分段/整合 text 每条时长必须落在该区间内。
- 交付形态：每条 text 自包含、复制即用——词头+资产引用+镜头时间轴全部在块内，不再需要任何拼装。
- 镜头描写细化：镜头段每镜补足动作细节、微表情、环境细节与光影层次；描写直白拆解到肢体细节（手指/血管/眼神/呼吸），不用成语和文学化修辞。
- 镜头参数：每镜标注焦距/光圈/景深——24–35mm 广角（空间/冲击）、50mm 标准叙事、85–135mm 长焦（情绪压缩/特写）、100mm 微距（细节放大）；f/1.8–f/2.8 控制景深；机位角度（贴地/仰拍15°/俯拍/肩平/侧面）。已写入技能 2.7 节。
- 定位关系：词头/资产段必带 `定位：` 行声明空间布局；ASCII 构图图（竖框=9:16 画面边界，[元素]标注方位，→标光源方向）只表达平面，必须追加「垂直剖面」行（元素高度 m、机位高度、视线方向）。已写入技能 2.8 节。
- 描写风格双模式（用户要求）：保留两种习惯——A 文学细腻（成语/意象/氛围）｜B 直白拆解（肢体细节/无成语）；每次生成提示词前用 ask_user_question 询问用户选哪种。两版结构完全一致，仅镜头行动作/表情措辞不同。

### 剧情红线（用户要求，全流程最高优先级）
- 不许修改剧情，所有内容围绕剧情展开：词头必带「严格按原剧情执行，不增删情节不改台词」。
- 剧本给出的集数固定不动（如 25 集），每集剧情保持原样；所有会更改原剧本内容（台词/情节/人物设定/集数/分集剧情/时间线）的方案一律不采用。
- 覆盖全流程四阶段：剧本诊断（不输出修改建议）/ 资产需求分析（资产只服务呈现）/ 分镜 / 提示词（台词照抄原剧本）。
- 台词精简度：待定、不使用其中的修改，但每次诊断仍输出台词「参考建议」（待定·仅供参考，是否采纳由用户决定）——诊断中唯一保留的建议输出。

### 用户偏好与纠正
- 画幅不需要写进提示词：由即梦参数面板设置（默认 9:16 竖屏）。
- 一镜一画面：一条提示词只能是一个镜头（单一机位+单一主体/动作组）；「动作+反应」「不同机位」必须拆成独立镜头。
- 无 BGM 为全局默认（仅环境音/音效/配音），除非用户脚本明确要求 BGM；示例中的「低声弦乐辅底」不采纳（只学格式不学 BGM）。
- 剧本诊断不输出「修改建议」模块。
- 反馈内容一律 text 模式（```text 代码块 + 纯文本，不用 Markdown 富排版），方便直接复制。
- 提示词稿格式（用户要求）：提示词稿文件也用纯 text 格式（无 Markdown 富语法，纯文本分隔线/编号，扩展名 .txt）。21 集稿已重写为 `.txt` 并删除旧 .md。
- 镜头内容细化（用户要求）：21 集镜头行再细化——动作分步骤（抓手/递饼干/拍背的分解）、表情到眉眼/嘴角/手指（指节发白、嘴唇抿紧成线、眼里亮起光）、环境光影层次（月光光斑/晨雾光尘/夕阳斜照）。已更新 `被转让的网恋女友-21集-提示词稿.txt`。
- 镜头参数细化（用户要求）：补齐景别词典（大远景ELS/远景LS/全景FS/中全景MLS/中景MS/中近景MCU/近景CU/特写BCU/大特写ECU/微距）、机位高度（贴地/低/腰/胸/肩平/高/顶拍/航拍）、角度（平视/仰俯拍15-45°/荷兰角/OTS/POV/正反打）、运镜（推拉摇移跟环绕甩/斯坦尼康/摇臂/轨道/升降/变焦）。已写入技能 2.7、prompt-templates.md，并应用到 21 集（每镜景别带中英+机位角度）。
- 镜头词典再丰富（用户要求）：补充构图法则（三分/中心/对称/对角线/引导线/框架/留白/前景遮挡/俯仰透视）、光学特性（广角畸变/长焦压缩/鱼眼/移轴/柔焦/眩光/暗角/探针）、光影类型（伦勃朗/侧逆光/轮廓光/顶光/底光/剪影/柔硬光/冷暖/高低调）、节奏速度（慢动作/快切/变速/延时/抽帧/定格）、转场（硬切/匹配剪辑/遮挡/缩放/甩镜/叠化/闪白闪黑/声音先入）。已写入技能 2.7、prompt-templates.md，复刻套件同步。
- 镜头动作时序准确（用户纠正）：镜头内多动作必须按时间先后（环境→动作→反应，用先/随后/接着/最后），细节与景别匹配（全景不写特写级细节），动作量与时长匹配。已写入技能 2.7、prompt-templates.md，并重写 21 集 19 镜为时序准确版。
- 环境与人物分离（用户纠正）：每个镜头「环境：」静态背景/光线/陈设 与「人物：」动作/神态 各成一段、不穿插。已写入技能 2.7 原则第 4 条、prompt-templates.md，21 集 19 镜重写为环境人物分离版。
- 同步环境变化规则（用户提问）：环境行可含环境动态（风/光影/雨雪/物体晃动），人物动作与环境动态同时发生时用「与此同时/伴随」在人物行绑定（如「她抬手抚摸他的脸颊；与此同时，清风拂过撩起发丝」），分清先后与同步。已写入技能 2.7 原则第 5 条、模板、CODEX_KNOWLEDGE.md，复刻套件同步。
- 屏幕文字/字幕一律后期贴片，提示词不写文字（AI 必乱码）。
- 「血泪」类极端面部效果改「泪水结冰晶」物理方案。

## 项目推进记录（2026-本会话）

- 项目「被转让的网恋女友」：读入 `被转让的网恋女友-西语.docx`（25 章中西对照本地化粗稿）→ 剧本诊断（8 项，无修改建议，台词参考待定）→ 建档 → 人物档案落库（5 角色深扒版：剧情行为证据链+细化 canonical，外观默认值待确认；妮娜需瘦身/胖两版锁脸）→ 资产需求分析落库（8 场景/4 道具/4 音频/2 风格，无 BGM）→ 第 21 集分镜+提示词已设计（3 条 text，风格 A 文学细腻，台词照抄原文）。
